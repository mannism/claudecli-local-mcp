# CLAUDE.md — claudecli-local-mcp

> **Scope rule:** If a session starts from this repo, work only on this project. Do not touch other repos unless the Owner explicitly says otherwise.

> Global rules (TypeScript, security, git workflow, accessibility baseline) are in ~/.claude/CLAUDE.md

Internal project: Slack bot that proxies messages to Claude via either the local `claude` CLI or the Anthropic API, with multi-account Gmail digest (`/digest`) and Owner Inbox (`/owner-inbox`) commands powered by GPT-5.2.

---

## Project Overview

| Item | Detail |
|---|---|
| Purpose | Internal MCP Slack setup — Claude-powered bot with Gmail digest and inbox management |
| Runtime | Node.js (CommonJS) |
| Transport | Slack Bolt — HTTP or Socket Mode |
| Claude modes | `USE_CLI=true` → local `claude` CLI / `USE_CLI=false` → Anthropic API |

---

## Project Structure

```
src/
  index.js          — Slack Bolt app; handles DMs, @mentions, /digest, /inbox
  claude.js         — Claude abstraction (CLI or API mode)
  gmail.js          — Multi-account Gmail OAuth client and email fetcher
  openai.js         — GPT summarisation and priority classification (loads prompt from file)
  emailCommand.js   — /digest command: arg parser, orchestrator, Slack formatter
  inboxCommand.js   — /inbox command: directory scanner, GPT analysis, Slack formatter
prompts/
  email-summary.md  — GPT system prompt for /digest email classification
  inbox-analysis.md — GPT system prompt for /inbox file analysis
scripts/
  gmail-auth.js     — One-time OAuth flow; use --account 1|2|3 to set up each account
.env.example        — Required environment variables
.releaserc.json     — semantic-release config
.github/workflows/release.yml  — GitHub Actions release pipeline
```

---

## Environment Variables

Copy `.env.example` to `.env` and fill in values. Never commit `.env`.

### Slack
| Variable | Required | Description |
|---|---|---|
| `SLACK_BOT_TOKEN` | Yes | Bot OAuth token (`xoxb-...`) |
| `SLACK_SIGNING_SECRET` | Yes | Slack app signing secret |
| `USE_SOCKET_MODE` | No | `true` for Socket Mode (no tunnel needed locally) |
| `SLACK_APP_TOKEN` | Socket Mode only | App-level token (`xapp-...`) |
| `PORT` | No | HTTP port, defaults to `3000` |

### Claude
| Variable | Required | Description |
|---|---|---|
| `USE_CLI` | No | `true` = local `claude` CLI, `false` = Anthropic API |
| `ANTHROPIC_API_KEY` | API mode only | Anthropic API key (`sk-ant-...`) |
| `CLAUDE_MODEL` | No | Defaults to `claude-sonnet-4-6` |

### Gmail (`/digest`)
| Variable | Required | Description |
|---|---|---|
| `GMAIL_CLIENT_ID` | Yes | Shared Google OAuth 2.0 client ID |
| `GMAIL_CLIENT_SECRET` | Yes | Shared Google OAuth 2.0 client secret |
| `GMAIL_ACCOUNT_N_EMAIL` | At least 1 | Email address for account N (N = 1, 2, 3) |
| `GMAIL_ACCOUNT_N_ALIAS` | At least 1 | Short alias used in Slack (e.g. `diana`) |
| `GMAIL_ACCOUNT_N_REFRESH_TOKEN` | At least 1 | Generated by `node scripts/gmail-auth.js --account N` |
| `GMAIL_ACCOUNT_N_DEFAULT_QUERY` | No | Default Gmail query; blank = all unread |

### OpenAI
| Variable | Required | Description |
|---|---|---|
| `OPENAI_API_KEY` | Yes | OpenAI API key (`sk-proj-...`) |
| `OPENAI_MODEL_EMAIL_SUMMARY` | No | Model for `/digest` email classification (default: `gpt-5.2`) |
| `OPENAI_MODEL_INBOX` | No | Model for `/owner-inbox` file analysis (default: `gpt-5-mini`) |

### File Inboxes (`/owner-inbox`)
| Variable | Required | Description |
|---|---|---|
| `OWNER_INBOX_PATH` | `/owner-inbox` only | Folder where the team drops files for the owner to review |

---

## Running Locally

```bash
npm install
cp .env.example .env   # fill in values
npm run dev            # node --watch (auto-restart on change)
```

One-time Gmail OAuth setup per account:
```bash
node scripts/gmail-auth.js --account 1   # generates GMAIL_ACCOUNT_1_REFRESH_TOKEN
node scripts/gmail-auth.js --account 2   # repeat for additional accounts
```

### Slash Commands

Register `/digest` and `/owner-inbox` in the Slack app settings (Features → Slash Commands). In Socket Mode, no Request URL is needed.

```
/digest diana                          # all unread for diana's account
/digest diana label:Important          # raw Gmail query
/digest                                # auto-selects if only 1 account configured
/inbox                                 # scans OWNER_INBOX_PATH for files from the team
```

---

## Release Pipeline

Merging to `main` triggers `.github/workflows/release.yml`, which runs `semantic-release` and automatically:
- Bumps `package.json` version
- Generates / updates `CHANGELOG.md`
- Creates a GitHub release with release notes
- Tags the commit (`v1.2.3`)
- The version badge in `README.md` updates automatically via GitHub

---

## Code Rules

### Error Handling
- Wrap every Anthropic API call, OpenAI API call, Gmail API call, and Slack API call in `try/catch`
- Log failures with metadata: `timestamp`, `service`, `message` — never log tokens or API keys
- On failure, respond to the user with a safe message; never expose stack traces

### Input Validation
- Treat all Slack message text as untrusted input
- Sanitize before passing to shell commands (CLI mode already escapes single quotes)

### Secrets
- All credentials via `.env` only — never hardcode
- `.env` is gitignored

### Code Style
- `camelCase` for functions and variables
- `UPPER_SNAKE_CASE` for constants
- Add JSDoc for any non-obvious logic

### Prompt Management
- System prompts live in `prompts/` — never inline them in source files
- **Every edit to a prompt file must be its own branch and commit** — never bundle prompt changes with code changes
- Prompt files are loaded at module startup via `fs.readFileSync` — restart the bot to pick up changes
